plugins {
    id 'java'
}

subprojects {

    group 'com.uzunalov'
    version '0.0.1'
    sourceCompatibility = '21'

    repositories {
        mavenLocal()
        mavenCentral()
        maven { url 'https://repo.spring.io/milestone' }
    }

    apply plugin: 'java'
    apply plugin: 'jacoco'
}